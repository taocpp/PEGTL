version: '{branch}-{build}'

os: Visual Studio 2015

configuration: Release

branches:
  only:
    - master

init: []

build_script:
- md build-vc
- cd build-vc
- cmake -Wno-dev ..
- make all

test_script:
- make test

install: []
