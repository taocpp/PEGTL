version: '{branch}-{build}'

os: Visual Studio 2015

configuration: Release

branches:
  only:
    - master

init: []

build_script:
- md build-vc
- cd build-vc
- cmake -Wno-dev ..
- cmake --build .

test_script:
- ctest -VV -C .

install: []
